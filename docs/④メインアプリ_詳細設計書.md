\*InventoryApp クラス（メインアプリ）

役割: CLI操作を受け付け、他クラスを呼び出す「司令塔」。*\

import java.util.*;

public class InventoryApp {
    private Inventory inventory = new Inventory();
    private FileManager fileManager = new FileManager();
    private Scanner scanner = new Scanner(System.in);

    public static void main(String[] args) {
        InventoryApp app = new InventoryApp();
        app.run();
    }

    public void run() {
        inventory = new Inventory();
        inventory.getAllProducts().addAll(fileManager.loadProducts());
        showMenu();
        fileManager.saveProducts(inventory.getAllProducts());
    }

    private void showMenu() { ... } // メニュー表示＆入力受付
    private void handleMenu(int choice) { ... } // 各操作処理
}
