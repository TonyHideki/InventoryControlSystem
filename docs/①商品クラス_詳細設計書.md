/*Product クラス（商品情報）

役割: 個々の商品の属性を保持するデータクラス*/

public class Product {
    private int id;
    private String name;
    private int quantity;
    private int price;

    // コンストラクタ
    public Product(int id, String name, int quantity, int price){ ... }

    // Getter/Setter
    public int getId(){ ... }
    public void setID(int id){ ... }
    // 以下同様に name, quantity, price

    // 表示用
    @Override
    public String.format("%-5d %-15s %-8d %-8d", id, name, quantity, price);


}
